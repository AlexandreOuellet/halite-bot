import hlt
import logging

def doAction(map, ship, action):
    return None
    # closestPlanet = _getClosestPlanet(map, ship)
    # return ship.dock(closestPlanet)

    # return ship.thrust(magnitude, angle)

    # return ship.undock()

    # return None
    # return ship.dock(planet)

def _getClosestPlanet(map, ship):
    return 0