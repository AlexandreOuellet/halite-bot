import numpy as np
import nnutils
# import keras

index = nnutils.parseCommandToActionIndex('t 0 7 90')

# rand = np.random.randint(10, size=(100, 1))
# # y_train = keras.utils.to_categorical(, num_classes=10)
# print(rand)

# import keras
# y_train = keras.utils.to_categorical(rand, num_classes=10)
# print(y_train)